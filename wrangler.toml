name = "blog-site"
compatibility_date = "2026-07-22"
pages_build_output_dir = "dist"

# Pages Functions support Workers AI (`env.AI`). Ask AI uses
# env.AI.autorag("stevenjhu-ai-search") against the AI Search instance.
[ai]
binding = "AI"
remote = true
