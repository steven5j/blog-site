---
interface Props {
  transparent?: boolean;
}

const { transparent = false } = Astro.props;
const pathname = Astro.url.pathname;

/** Desktop primary nav — mirrors WP OceanWP menu */
const primaryLinks = [
  { href: '/', label: 'Home', match: 'exact' as const, icon: 'home' as const },
  { href: '/about', label: 'About', match: 'prefix' as const },
  {
    href: '/blog',
    label: 'Blog',
    match: 'blog' as const,
    children: [
      { href: '/topics/software', label: '軟體工程筆記' },
      { href: '/topics/business', label: '產業投資與商業經驗' },
      { href: '/topics/life', label: '生活心得體驗分享' },
    ],
  },
];

/** Extra routes — available in mobile overlay */
const extraLinks = [
  { href: '/projects', label: 'Projects', match: 'prefix' as const },
  { href: '/series', label: 'Study Notes', match: 'prefix' as const },
];

const allLinks = [...primaryLinks, ...extraLinks];

function isDatePermalink(path: string) {
  return /^\/\d{4}\/\d{2}\/\d{2}(\/|$)/.test(path);
}

function isActive(href: string, match: 'exact' | 'prefix' | 'blog') {
  if (match === 'exact') return pathname === '/' || pathname === '';
  if (match === 'blog') {
    return (
      pathname === '/blog' ||
      pathname.startsWith('/blog/') ||
      pathname.startsWith('/topics/') ||
      isDatePermalink(pathname)
    );
  }
  return pathname === href || pathname.startsWith(`${href}/`);
}
---

<header
  class:list={[
    'site-header z-40 w-full',
    transparent
      ? 'absolute inset-x-0 top-0 bg-transparent'
      : 'relative bg-ink text-white',
  ]}
>
  <div
    class="mx-auto flex max-w-site items-center justify-between gap-4 px-5 py-4 sm:px-6 lg:px-8"
  >
    <a
      href="/"
      class="font-display text-lg font-semibold tracking-tight text-brand no-underline transition hover:text-brand-hover sm:text-xl"
      data-astro-prefetch
    >
      Steven玄
    </a>

    <!-- Desktop: WP-style horizontal nav -->
    <nav class="hidden md:block" aria-label="主要導覽">
      <ul class="flex items-center gap-1 lg:gap-2">
        {
          primaryLinks.map((link) => (
            <li class="group relative">
              <a
                href={link.href}
                class:list={[
                  'inline-flex items-center gap-2 px-3 py-2 text-[15px] no-underline transition',
                  isActive(link.href, link.match)
                    ? 'text-brand'
                    : 'text-white/90 hover:text-brand',
                ]}
                aria-current={isActive(link.href, link.match) ? 'page' : undefined}
                data-astro-prefetch
              >
                {'icon' in link && link.icon === 'home' ? (
                  <span class="sr-only">{link.label}</span>
                ) : (
                  link.label
                )}
                {'icon' in link && link.icon === 'home' && (
                  <svg
                    class="h-[1.35rem] w-[1.35rem]"
                    viewBox="0 0 24 24"
                    fill="none"
                    stroke="currentColor"
                    stroke-width="1.75"
                    stroke-linecap="round"
                    stroke-linejoin="round"
                    aria-hidden="true"
                  >
                    <path d="M3 10.5 12 3l9 7.5" />
                    <path d="M5 10v10h14V10" />
                    <path d="M10 20v-6h4v6" />
                  </svg>
                )}
              </a>
              {'children' in link && link.children && (
                <ul
                  class="invisible absolute right-0 top-full z-50 min-w-[14rem] translate-y-1 border border-white/10 bg-ink py-2 opacity-0 shadow-lg transition group-hover:visible group-hover:translate-y-0 group-hover:opacity-100 group-focus-within:visible group-focus-within:translate-y-0 group-focus-within:opacity-100"
                >
                  {link.children.map((child) => (
                    <li>
                      <a
                        href={child.href}
                        class="block whitespace-nowrap px-4 py-2.5 text-sm text-white/80 no-underline transition hover:bg-white/5 hover:text-brand"
                        data-astro-prefetch
                      >
                        {child.label}
                      </a>
                    </li>
                  ))}
                </ul>
              )}
            </li>
          ))
        }
      </ul>
    </nav>

    <!-- Mobile only: hamburger -->
    <button
      type="button"
      class="menu-toggle inline-flex cursor-pointer items-center gap-2 border-0 bg-transparent p-1 text-sm uppercase tracking-[0.15em] text-white/90 md:hidden"
      aria-expanded="false"
      aria-controls="site-menu"
      aria-label="開啟選單"
      data-menu-toggle
    >
      <span class="flex flex-col gap-1" aria-hidden="true">
        <span class="block h-0.5 w-5 bg-current"></span>
        <span class="block h-0.5 w-5 bg-current"></span>
        <span class="block h-0.5 w-5 bg-current"></span>
      </span>
      <span>Menu</span>
    </button>
  </div>

  <!-- Mobile overlay -->
  <nav
    id="site-menu"
    class="site-menu fixed inset-0 z-50 hidden bg-ink/95 text-white backdrop-blur-sm md:hidden"
    aria-label="手機導覽"
    data-menu-panel
    hidden
  >
    <div class="mx-auto flex h-full max-w-lg flex-col px-8 py-10">
      <div class="mb-10 flex items-center justify-between">
        <span class="font-display text-lg font-semibold text-brand">Steven玄</span>
        <button
          type="button"
          class="cursor-pointer border-0 bg-transparent text-sm uppercase tracking-[0.15em] text-white/80"
          data-menu-close
        >
          Close
        </button>
      </div>
      <ul class="flex flex-col gap-1">
        {
          allLinks.map((link) => (
            <li>
              <a
                href={link.href}
                class:list={[
                  'block py-3 font-display text-2xl font-medium no-underline transition hover:text-brand',
                  isActive(link.href, link.match) ? 'text-brand' : 'text-white',
                ]}
                aria-current={isActive(link.href, link.match) ? 'page' : undefined}
                data-astro-prefetch
                data-menu-close
              >
                {link.label}
              </a>
              {'children' in link && link.children && (
                <ul class="mb-2 ml-1 border-l border-white/15 pl-4">
                  {link.children.map((child) => (
                    <li>
                      <a
                        href={child.href}
                        class="block py-2 text-sm text-white/70 no-underline transition hover:text-brand"
                        data-astro-prefetch
                        data-menu-close
                      >
                        {child.label}
                      </a>
                    </li>
                  ))}
                </ul>
              )}
            </li>
          ))
        }
      </ul>
    </div>
  </nav>
</header>

<script>
  function bindMenu() {
    const toggle = document.querySelector<HTMLButtonElement>('[data-menu-toggle]');
    const panel = document.querySelector<HTMLElement>('[data-menu-panel]');
    if (!toggle || !panel) return;

    const open = () => {
      panel.hidden = false;
      panel.classList.remove('hidden');
      toggle.setAttribute('aria-expanded', 'true');
      document.body.style.overflow = 'hidden';
    };
    const close = () => {
      panel.hidden = true;
      panel.classList.add('hidden');
      toggle.setAttribute('aria-expanded', 'false');
      document.body.style.overflow = '';
    };

    toggle.onclick = () => {
      if (panel.hidden) open();
      else close();
    };
    panel.querySelectorAll('[data-menu-close]').forEach((el) => {
      el.addEventListener('click', close);
    });
    panel.addEventListener('keydown', (e) => {
      if (e.key === 'Escape') close();
    });

    // Close overlay if resized to desktop
    const mq = window.matchMedia('(min-width: 768px)');
    const onMq = () => {
      if (mq.matches) close();
    };
    mq.addEventListener('change', onMq);
  }

  bindMenu();
  document.addEventListener('astro:page-load', bindMenu);
</script>
